
<% @genres.each do |genre| %>
    <h1><%= genre.name %> </h1>
    <% genre.games.each do |game| %>
      <p>
        <%= link_to image_tag(game.thumbnail, width: 150, height: 150 ), game  %><br>
        <%= game.title %>
      </p>
  <% end %>
  <% end %>